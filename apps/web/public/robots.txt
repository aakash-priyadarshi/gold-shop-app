# ─── Orivraa Custom Rules ─────────────────────────────────────────
# NOTE: Cloudflare prepends its own managed section above this file
# with AI training bot blocks and Content-Signal headers.
# Our rules below extend the Cloudflare-managed User-agent: * block.
# Do NOT add another "User-agent: *" / "Allow: /" here — Cloudflare handles it.

# Disallow sensitive/private areas (applies to all crawlers)
User-agent: *
Disallow: /api/
Disallow: /dashboard/
Disallow: /auth/
Disallow: /cart
Disallow: /checkout
Disallow: /orders
Disallow: /notifications
Disallow: /admin/

# NOTE: Do NOT disallow /_next/ — Googlebot needs /_next/static/
# to render JavaScript and see page content for proper indexing.

# ─── Allow Google AI (Gemini) to read content ────────────────────
# Cloudflare blocks Google-Extended by default for AI training.
# Override: explicitly allow it so Gemini can understand & cite Orivraa.
User-agent: Google-Extended
Allow: /

# ─── Block known bad/spam bots ───────────────────────────────────
User-agent: MJ12bot
Disallow: /

User-agent: DotBot
Disallow: /

# ─── Sitemap ─────────────────────────────────────────────────────
Sitemap: https://www.orivraa.com/sitemap.xml
