You are a senior full-stack engineer and product UI builder.

CONTEXT
I have a jewellery marketplace app already deployed:
- Frontend: Next.js (App Router) on Vercel
- Backend: NestJS on Railway
- DB: Neon Postgres (Prisma)
- Cache: Upstash Redis

We already have pricing endpoints working and market-rates logic. DO NOT break these.
Now we must add ACCOUNTS + ROLE-BASED DASHBOARDS.

CRITICAL INSTRUCTION (READ CAREFULLY)
Claude, in the previous attempt you did NOT create most of the required pages.
This time you MUST:
1) Create EVERY page route listed below (actual files, not placeholders in text).
2) Create dashboard layouts + sidebar nav + topbar + starter widgets.
3) Implement role-based routing protection (server + client safe).
4) Provide the final answer as: “Plan” + “Exact file tree changes” + “Full code for each new/modified file”.
5) Before finishing, VERIFY that every page exists by listing them and mapping each one to a file path.

ROLE TYPES
- ADMIN: platform operator
- SHOPKEEPER: shop owner/manager
- CUSTOMER: end user buying / submitting RFQs

UX GOAL
After login, redirect user to their dashboard.
A user MUST NOT be able to access other roles’ dashboard pages (both frontend route guard and backend guard).
If they try, they get redirected to the correct dashboard or shown Forbidden.

==========================================================
A) BACKEND REQUIREMENTS (NestJS)
==========================================================
A1) Prisma Data Model
Ensure these models exist (or update them) in prisma/schema.prisma.

User:
- id (uuid)
- email (unique)
- phone (optional)
- name
- passwordHash
- role enum: ADMIN | SHOPKEEPER | CUSTOMER
- isActive boolean default true
- createdAt/updatedAt

Shop:
- id (uuid)
- ownerId (FK -> User.id) UNIQUE (SHOPKEEPER must own exactly one shop)
- shopName
- country (2-letter code: IN, NP, US, UK, EU, AE)
- currency (enum supported: INR, NPR, USD, GBP, EUR, AED)
- address
- isVerified boolean default false
- createdAt/updatedAt

RefreshToken (rotation table):
- id uuid
- userId FK
- tokenHash
- expiresAt
- revokedAt nullable
- createdAt

Constraints:
- Only SHOPKEEPER can own a shop.
- A SHOPKEEPER must have exactly one shop (enforce via unique ownerId + creation flow).
- CUSTOMER/ADMIN have no shop.

A2) Auth Endpoints (Production grade)
Implement:
- POST /api/auth/register
  * CUSTOMER: direct register
  * SHOPKEEPER: register + shop details in same request OR 2-step flow (register then create shop) — choose the safer approach and implement.
  * ADMIN: NOT publicly registerable (only seed or created by admin)
- POST /api/auth/login
- POST /api/auth/refresh
- POST /api/auth/logout
- GET /api/auth/me  -> returns { user, role, shopSummary? }

JWT:
- Access token short-lived (e.g., 15m)
- Refresh token long-lived (e.g., 30d) stored as httpOnly cookie
- Refresh token rotation using RefreshToken table
- Logout revokes refresh token (sets revokedAt)

A3) Guards & Decorators
- Roles decorator: @Roles('ADMIN', 'SHOPKEEPER', 'CUSTOMER')
- RolesGuard checks req.user.role
- Also block users with isActive=false
- Return:
  * 401 if no auth
  * 403 if wrong role
- Protect:
  * /api/admin/** only ADMIN
  * /api/shops/my-shop/** only SHOPKEEPER
  * /api/orders/my-orders and /api/rfq/my-requests only CUSTOMER
  * existing pricing endpoints remain public or as currently designed (do not change unless needed)

A4) Admin seed
- On bootstrap or script:
  - If ADMIN_EMAIL and ADMIN_PASSWORD env vars exist, create admin if missing.
  - Password must be hashed.

A5) Admin/Shopkeeper/Customer minimal endpoints to power dashboards
Create minimal endpoints needed for dashboards:
ADMIN:
- GET /api/admin/kpis -> returns placeholder metrics (counts from DB; revenue = 1% of GMV)
- GET /api/admin/shops/pending -> isVerified=false list
- PATCH /api/admin/shops/:id/verify (approve/deny)
- GET /api/admin/users -> list users with role + status
- PATCH /api/admin/users/:id/suspend /activate

SHOPKEEPER:
- GET /api/shops/my-shop -> shop details
- GET /api/shops/my-shop/dashboard -> {kpis placeholders, recent orders, pending rfqs}
- GET /api/orders/shop/:shopId -> list orders (or /api/orders/shop/my if you prefer)
- GET /api/rfq/shop-requests -> list incoming RFQs
- POST /api/offers -> submit offer for RFQ (if already exists, integrate)

CUSTOMER:
- GET /api/rfq/my-requests -> list
- GET /api/orders/my-orders -> list

IMPORTANT: If any of these already exist, reuse them. Don’t duplicate.

==========================================================
B) FRONTEND REQUIREMENTS (Next.js App Router)
==========================================================
We use Next.js app router (app/ directory).
Build production-grade pages and layouts.

B1) Auth UI pages (MUST CREATE FILES)
- /login
- /register
- /logout (optional route that calls logout and redirects)

Registration flow UI:
- Step selector: “I am a Customer” vs “I am a Shopkeeper”
CUSTOMER register fields:
  - name, email, password, confirm password
SHOPKEEPER register fields:
  - ownerName, email, password, confirm password
  - shopName, country dropdown, defaultCurrency dropdown, address
After successful SHOPKEEPER registration:
  - redirect to /dashboard/shop
  - show “Verification pending” banner in dashboard

ADMIN:
- no public registration page option for ADMIN.

B2) Auth State Management (must be implemented fully)
- Create an AuthProvider at app level:
  - On load: call /api/auth/me
  - If 401: call /api/auth/refresh once, then retry /me
- Store access token in memory (React state) OR in secure storage if you must, but prefer in-memory.
- All API calls should send credentials so refresh cookie works.
- Create hook useAuth() => { user, role, shop, loading, login(), registerCustomer(), registerShopkeeper(), logout() }

B3) Role-based routing protection (MUST DO BOTH)
1) Client/Layout guard:
  - If not authed => redirect /login
  - If wrong role => redirect to correct dashboard
2) Next middleware guard (edge):
  - For /dashboard/admin/* require role=ADMIN
  - For /dashboard/shop/* require role=SHOPKEEPER
  - For /dashboard/customer/* require role=CUSTOMER
  Implementation note:
  - If role is not readable from cookie, then middleware should at least ensure auth cookie exists and then rely on layout guard.
  - If you store role in a non-httpOnly cookie for routing (acceptable), do it securely and keep it minimal (role + isAuthed boolean).
  - Do not expose access token in JS-readable cookie.

B4) DASHBOARD ROUTES AND PAGES (MUST CREATE ALL FILES)
You MUST implement these exact routes with real UI:

ADMIN DASHBOARD
- /dashboard/admin
  UI content:
  - KPI cards: total users, total shops, total orders, total GMV, revenue(1%)
  - “Pending shop approvals” preview (top 5)
  - “Recent signups” preview (top 5)
- /dashboard/admin/shops
  UI content:
  - Tabs: Pending / Verified / All
  - Each shop row: shopName, owner, country, currency, status badge
  - Pending shops have Approve + Deny buttons (calls admin verify endpoint)
- /dashboard/admin/users
  UI content:
  - User table with filters: role dropdown, active/suspended
  - Actions: Suspend/Activate
- /dashboard/admin/settings
  UI content:
  - Platform fee: default 1% (display only)
  - Supported regions/currencies list (display)
  - Basic “System defaults” placeholder section

SHOPKEEPER DASHBOARD
- /dashboard/shop
  UI content:
  - Banner: Verification status (Verified / Pending)
  - My Shop summary card (name, country, currency)
  - KPIs placeholder: orders this month, revenue this month, pending RFQs
  - Quick actions: “View Orders”, “View RFQs”, “Pricing Settings”
- /dashboard/shop/orders
  UI content:
  - Orders table: orderId, customer, total, status, createdAt
  - Click row => /dashboard/shop/orders/[id]
- /dashboard/shop/orders/[id]
  UI content:
  - Order details layout with status timeline placeholder
- /dashboard/shop/rfqs
  UI content:
  - RFQ list: jewelleryType, weight, budget, status, createdAt
  - Click => /dashboard/shop/rfqs/[id]
- /dashboard/shop/rfqs/[id]
  UI content:
  - RFQ details
  - Offer form: price, deliveryDays, notes (submit to /api/offers)
- /dashboard/shop/pricing
  UI content:
  - “Default making charge %” (input)
  - “Margin %” (input)
  - “Overrides table” placeholder (metal type, percent)
  - Save button (store in DB later; for now can save to backend placeholder endpoint or local state, but you MUST create the page and UI)

CUSTOMER DASHBOARD
- /dashboard/customer
  UI content:
  - Quick Price Estimate widget:
    - Country selector (market)
    - Currency selector (view currency)
    - jewelleryType selector
    - weight input
    - makingChargePct input
    - calls /api/pricing/estimate and shows breakdown
  - “My RFQs” preview (top 5)
  - “My Orders” preview (top 5)
- /dashboard/customer/rfqs
  UI content:
  - RFQ table + “Create RFQ” button -> /dashboard/customer/rfqs/new
- /dashboard/customer/rfqs/new
  UI content:
  - RFQ create form (minimal fields)
- /dashboard/customer/orders
  UI content:
  - Orders list + status pill
- /dashboard/customer/settings
  UI content:
  - Profile info (read-only initially)
  - Preferred currency dropdown (save to backend later; but UI must exist)

B5) UI LAYOUT REQUIREMENTS
- Each dashboard uses a shared layout:
  - Sidebar with nav links for that role (Admin / Shop / Customer)
  - Top bar showing user name + role + logout button
- Styling: simple clean UI using your current component system (if none, use basic Tailwind).
- Must be responsive (sidebar collapses on mobile).

B6) API CLIENT (single wrapper)
- Create /lib/api.ts that wraps fetch:
  - baseURL from env
  - includes credentials
  - handles 401 -> tries refresh -> retries once
- Add typed response interfaces for /me and dashboard endpoints.

==========================================================
C) DELIVERABLES AND COMPLETION CONTRACT
==========================================================
C1) You MUST output:
1) File tree of all new/modified files
2) Code for each file (full content)
3) Clear env vars list (backend + frontend)
4) Deployment steps for Railway/Vercel/Neon

C2) STOP CONDITION CHECKLIST
Before you finish, you MUST include a checklist and confirm each is implemented:

AUTH PAGES
- [ ] /login exists and works
- [ ] /register exists and supports customer + shopkeeper flow
- [ ] Role-based redirect after login

ADMIN PAGES
- [ ] /dashboard/admin
- [ ] /dashboard/admin/shops
- [ ] /dashboard/admin/users
- [ ] /dashboard/admin/settings

SHOPKEEPER PAGES
- [ ] /dashboard/shop
- [ ] /dashboard/shop/orders
- [ ] /dashboard/shop/orders/[id]
- [ ] /dashboard/shop/rfqs
- [ ] /dashboard/shop/rfqs/[id]
- [ ] /dashboard/shop/pricing

CUSTOMER PAGES
- [ ] /dashboard/customer
- [ ] /dashboard/customer/rfqs
- [ ] /dashboard/customer/rfqs/new
- [ ] /dashboard/customer/orders
- [ ] /dashboard/customer/settings

GUARDS
- [ ] Backend RolesGuard and decorators working
- [ ] Frontend dashboard layout guard working
- [ ] Middleware protection implemented (at least basic)

==========================================================
D) IMPORTANT CONSTRAINTS
==========================================================
- Do not break existing pricing endpoints.
- Keep “country decides market but user can view in any currency” working.
- Use consistent role enum naming across FE + BE.
- Add validation DTOs with class-validator.
- No hardcoded secrets.

Now implement everything.
Do NOT give suggestions only — write the code and file changes.
